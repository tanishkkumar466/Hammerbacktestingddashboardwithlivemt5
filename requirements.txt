# Hammer Candle Dashboard — runtime libraries
# Requires Python 3.11 (same version used to build the Windows .exe).
#
#   python3.11 -m pip install -r requirements.txt
#
# Do not install with 3.12/3.13 for the packaged app — stick to 3.11.

# --- UI ---
PySide6>=6.6

# --- Backtest, indicators, charts, Excel export ---
matplotlib>=3.8
# MUST use polars-lts-cpu for the Windows exe — plain `polars` is built with AVX/AVX2
# and dies with Windows fatal exception 0xC000001D (illegal instruction) on many PCs.
# Same import: `import polars as pl`. Do NOT also install `polars`.
polars-lts-cpu>=0.20
pyarrow>=15.0
numpy>=1.26
pillow>=10.0
openpyxl>=3.1
et-xmlfile>=1.1
tzdata>=2024.1

# --- Live trading ---
psutil>=5.9
# [default] extras — aiohttp, pydantic, virtualenv, etc. (full ~430 MB exe)
ray[default]>=2.9
# Official MetaTrader5 wheel is Windows-only (live MT5 + fetch.py).
MetaTrader5>=5.0.45; sys_platform == "win32"

# --- HTTPS (updater + Telegram + ray) ---
certifi
requests
