# Hammer Candle Dashboard — Python 3.11 FULL Windows build / PyInstaller
# GitHub Actions and local Windows packaging must use 3.11.
#
#   python3.11 -m pip install -r requirements-build.txt
#   pyinstaller --noconfirm --clean HammerCandleBacktestDashboard.spec
#
# Installs EVERY runtime + transitive library Hammer may import on Windows 10/11.
# Target one-file exe: ~420–450 MB (matches local working builds).

-r requirements.txt

setuptools>=69
wheel>=0.43
# MUST match the local Windows build that actually opens (user-verified: 6.19).
# Do NOT use 6.21/6.22+ here — different bootloader than the known-good local exe.
# 6.22.1+ also adds parent-process security checks that break self-update.
pyinstaller==6.19.0


# =============================================================================
# HTTPS / networking (updater, Telegram, ray)
# =============================================================================
certifi
charset-normalizer
idna
urllib3
requests

# =============================================================================
# matplotlib full stack (PNG charts + Qt inspect dialog)
# =============================================================================
kiwisolver
fonttools
contourpy
cycler
pyparsing
packaging
python-dateutil
six

# =============================================================================
# Excel export chain
# =============================================================================
defusedxml

# =============================================================================
# polars / pyarrow / numpy IO
# =============================================================================
fsspec

# =============================================================================
# Ray CORE (always required when ray is enabled in live)
# =============================================================================
cloudpickle
filelock
jsonschema
jsonschema-specifications
msgpack
protobuf
grpcio
PyYAML
click

# =============================================================================
# Ray [default] extras — MUST match local ~430 MB exe (not core-only ~408 MB)
# =============================================================================
aiohttp
aiohttp-cors
aiohappyeyeballs
aiorwlock
aiosignal
annotated-types
attrs
colorful
distlib
frozenlist
multidict
opencensus
opencensus-context
platformdirs
prometheus-client
pydantic>=2.5,<3
pydantic-core
referencing
rich
rpds-py
smart-open
typing-extensions
virtualenv
watchfiles
yarl

# =============================================================================
# Rich / jsonschema / virtualenv / grpc transitive
# =============================================================================
markdown-it-py
mdurl
pygments
googleapis-common-protos
proto-plus
google-api-core
google-auth
cachetools
pyasn1
pyasn1-modules
rsa

# =============================================================================
# Extra ray/grpc helpers (Windows-safe; bundled if installed)
# =============================================================================
lz4
ormsgpack
